# Cloudflare Workers Static Assets (SPA)
# Dashboard: build from monorepo root, then deploy this config.
name = "sonari-admin"
compatibility_date = "2025-07-04"
workers_dev = true

[assets]
directory = "./dist"
not_found_handling = "single-page-application"
